const LOAD_STATUS = Object.freeze({
  QUEUED: 'QUEUED',
  VALIDATING: 'VALIDATING',
  LOADING: 'LOADING',
  COMPLETED: 'COMPLETED',
  FAILED: 'FAILED',
});

const WRITE_DISPOSITION = Object.freeze({
  APPEND: 'WRITE_APPEND',
  EMPTY: 'WRITE_EMPTY',
  TRUNCATE: 'WRITE_TRUNCATE',
});

const ACTIVE_STATUSES = Object.freeze([
  LOAD_STATUS.QUEUED,
  LOAD_STATUS.VALIDATING,
  LOAD_STATUS.LOADING,
]);

module.exports = {
  LOAD_STATUS,
  WRITE_DISPOSITION,
  ACTIVE_STATUSES,
};
