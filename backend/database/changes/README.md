# Database Changes

Put manually applied production database changes here using dated filenames, for example:

`2026-08-11_add_new_conversion_format.sql`

After applying a change, update `database/schema/dataforge.sql` so it remains the latest full schema snapshot.
